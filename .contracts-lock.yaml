# 演習用 consumer lock（違反ケース — contract_pin 期待 exit 1）
# order-api 1.0.0 は既に 2.0.0 が公開されており完全一致 pin が満たされない
contracts:
  - name: order-api
    version: "1.0.0"
    source: control
  - name: inventory-api
    range: ">=3.0.0"
    source: control
